description: Acesso não autorizado
content:
  application/json:
    type: object
    properties:
      status:
        type: integer
        format: int32
      error:
        type: integer
        format: int32
      msg:
        type: string
    example: {
      'status': 401,
      'message': 'Forneça um token válido para acessar a API.'
    }