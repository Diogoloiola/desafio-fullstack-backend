description: Endpoint que excluí o usuario
content:
  application/json:
    type: object
    properties:
      status:
        type: integer
        format: int32
      error:
        type: integer
        format: int32
      msg:
        type: string
    example: {
      'status': 202,
      'message': 'Usuário excluído com sucesso.'
    }